{
  "name": "axiom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "lint": "pnpm run -r --if-present lint",
    "lint:fix": "pnpm run -r --if-present lint:fix",
    "test": "pnpm run -r --if-present test",
    "build": "pnpm run -r --if-present build",
    "publish:ci": "pnpm -r publish",
    "version:patch": "pnpm run -r --if-present version:patch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
      "@dsmrt/axiom-cli": "workspace:^"
  },
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
