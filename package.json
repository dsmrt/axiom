{
  "name": "axiom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "pnpm run -r --if-present lint",
    "lint:fix": "pnpm run -r --if-present lint:fix",
    "test": "pnpm run -r --if-present test",
    "build": "pnpm run -r --if-present build",
    "publish": "pnpm run -r --if-present publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
